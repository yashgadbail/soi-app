// Placeholder replaced by the real suites under test/unit, test/widget and
// test/golden. Kept so `flutter test` has a target until they land.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test harness boots', () {
    expect(1 + 1, 2);
  });
}
